# EGSPO-CA v2 Requirements
# For GPU server (SenseTime / 4090_hyl)

# Core
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
datasets>=2.19.0
peft>=0.10.0

# RL training
trl>=0.9.0  # For GRPO (if using TRL implementation)

# Math / utils
numpy>=1.24.0
scipy>=1.12.0
scikit-learn>=1.4.0  # For R^2 computation

# Logging
wandb>=0.16.0  # Optional: for experiment tracking

# Data processing
jsonlines>=4.0.0
tqdm>=4.66.0

# Evaluation
evaluate>=0.4.0
rouge-score>=0.1.2

# Optional: verl (for distributed training, as used in paper)
# git+https://github.com/volcengine/verl.git
